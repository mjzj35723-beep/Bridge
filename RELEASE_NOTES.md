# BRIDGE 0.1.0

This package contains the canonical native branch-likelihood pullback implementation, the original three-dataset command, a configurable six-dataset runner, dataset/model download helpers, and environment/reproduction documentation.

The release intentionally excludes model weights, tensor caches, generated results and outputs, runtime logs, gated SorryBench data, private credentials, and machine-specific absolute paths.
